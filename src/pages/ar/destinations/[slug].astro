---
import DestinationDetail from '../../../views/DestinationDetail.astro';
import { getDestination } from '../../../lib/content';
const { slug } = Astro.params;
if (!(await getDestination(slug!))) return new Response(null, { status: 404 });
---
<DestinationDetail locale="ar" slug={slug!} />
